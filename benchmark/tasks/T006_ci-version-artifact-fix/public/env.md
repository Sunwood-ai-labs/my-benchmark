# Environment

- 複数ファイルに分散した version truth を追う力を測る。

## 関連ファイル・前提
- benchmark runner は `repo_with_release_or_ci_workflow` に対応する fixture または local repo を与える前提です。
- `public/fixtures/` には最小再現に必要なログ、断片ファイル、再現手順のみを置く想定です。
- 評価対象エージェントは、編集前に既存のローカル指示や README を確認することが期待されます。
